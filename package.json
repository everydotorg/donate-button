{
  "scripts": {
    "build": "yarn workspace @everydotorg/donate-button build && cp -r packages/donate-button/dist ./docs"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^2.2.1",
    "xo": "^0.37.1"
  }
}
